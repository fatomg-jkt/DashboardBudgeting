import { db } from '@/lib/db'; import { NextResponse } from 'next/server'; export const runtime='nodejs';
export async function GET(){try{return NextResponse.json(await db('budget_requests?select=*&order=created_at.desc'))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Gagal membaca pengajuan.'},{status:500})}}
export async function POST(req:Request){try{const r=await req.json();if(!r.request_date||!r.department||!r.category||!Number.isFinite(Number(r.amount)))return NextResponse.json({error:'Data pengajuan tidak lengkap.'},{status:400});return NextResponse.json(await db('budget_requests',{method:'POST',body:JSON.stringify({...r,amount:Number(r.amount)})}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:'Gagal menyimpan pengajuan.'},{status:500})}}
