# Public project URL. Safe to expose; used as a server-side fallback.
NEXT_PUBLIC_SUPABASE_URL=
# Optional server-only URL alias. Use this instead of the public URL if supplied by your integration.
SUPABASE_URL=
# Server-only secret. Never prefix this variable with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
